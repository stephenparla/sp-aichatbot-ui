name: ui
type: Static Site

files:
  source: dist

http:
  alias: theprohub.org
  # 🔄 Essential for React Router (Login, Home, etc.)
  error_pages:
    - status_code: 404
      response_page_path: /index.html
      response_code: 200
